---
id: DATZI-057
category: cli
slug: memory
title: `datzi memory`
summary: Manage semantic memory indexing and search.
tags: ["cli", "memory"]
---

Source: https://docs.datzi.ai/cli/memory

# `datzi memory`

Manage semantic memory indexing and search.
Provided by the active memory plugin (default: `memory-core`; set `plugins.slots.memory = "none"` to disable).

Related:

- Memory concept: [Memory](/concepts/memory)
- Plugins: [Plugins](/tools/plugin)

## Examples

```bash
datzi memory status
datzi memory status --deep
datzi memory status --deep --index
datzi memory status --deep --index --verbose
datzi memory index
datzi memory index --verbose
datzi memory search "release checklist"
datzi memory status --agent main
datzi memory index --agent main --verbose
```

## Options

Common:

- `--agent <id>`: scope to a single agent (default: all configured agents).
- `--verbose`: emit detailed logs during probes and indexing.

Notes:

- `memory status --deep` probes vector + embedding availability.
- `memory status --deep --index` runs a reindex if the store is dirty.
- `memory index --verbose` prints per-phase details (provider, model, sources, batch activity).
- `memory status` includes any extra paths configured via `memorySearch.extraPaths`.

# message
