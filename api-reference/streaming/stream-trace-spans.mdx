---
openapi: 'GET /api/v1/stream/traces/{traceId}/spans'
---

This endpoint uses **Server-Sent Events (SSE)** for real-time streaming. Keep the connection open to receive span events for a specific trace as they occur.
