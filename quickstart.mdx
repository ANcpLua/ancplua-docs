---
title: "Quickstart"
description: "Add ANcpLua.NET.Sdk to your project"
---

## Prerequisites

- .NET SDK 8.0+
- Central Package Management enabled

```xml title="Directory.Packages.props"
<Project>
  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
  </PropertyGroup>
</Project>
```

## Installation

**1. Add SDK reference:**

```json title="global.json"
{
  "msbuild-sdks": {
    "ANcpLua.NET.Sdk": "1.3.31"
  }
}
```

**2. Update project file:**

<Tabs>
  <Tab title="Library/Console">
```xml
<Project Sdk="ANcpLua.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>
</Project>
```
  </Tab>
  <Tab title="Web API">
```xml
<Project Sdk="ANcpLua.NET.Sdk.Web">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>
</Project>
```
  </Tab>
  <Tab title="Tests">
```xml
<Project Sdk="ANcpLua.NET.Sdk.Test">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>
</Project>
```
  </Tab>
</Tabs>

**3. Build:**

```bash
dotnet build
```

## Auto-Configured

The SDK enables:
- Nullable reference types
- LangVersion `latest`
- Deterministic builds
- ANcpLua.Analyzers + Meziantou.Analyzer
- Banned API enforcement

## Multi-Targeting

```xml
<Project Sdk="ANcpLua.NET.Sdk">
  <PropertyGroup>
    <TargetFrameworks>net10.0;netstandard2.0</TargetFrameworks>
  </PropertyGroup>
</Project>
```

Polyfills auto-injected for `netstandard2.0`/`net472`:
- Index/Range (`^1`, `..`)
- `IsExternalInit`
- String extensions

See [Polyfills](/sdk/polyfills) for details.
