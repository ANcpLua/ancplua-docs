---
title: 'Das Auge Saurons'
description: 'One does not simply walk into Mordor... aber deployen in 2 Minuten geht schon.'
icon: 'eye'
noindex: true
---

<div style={{
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center',
  minHeight: '75vh',
  background: 'radial-gradient(ellipse at center, #1a0a00 0%, #0a0a0a 60%, #000000 100%)',
  borderRadius: '16px',
  overflow: 'hidden',
  position: 'relative',
  padding: '2rem'
}}>

<style>{`
    @keyframes flicker {
      0%, 100% { opacity: 0.6; transform: scale(1); }
      25% { opacity: 1; transform: scale(1.05); }
      50% { opacity: 0.8; transform: scale(0.98); }
      75% { opacity: 0.95; transform: scale(1.02); }
    }
    @keyframes pulse-glow {
      0%, 100% {
        box-shadow:
          0 0 60px 20px rgba(255,80,0,0.4),
          0 0 120px 40px rgba(255,40,0,0.2),
          inset 0 0 30px rgba(255,100,0,0.3);
      }
      50% {
        box-shadow:
          0 0 80px 30px rgba(255,100,0,0.6),
          0 0 160px 60px rgba(255,60,0,0.3),
          inset 0 0 40px rgba(255,120,0,0.4);
      }
    }
    @keyframes pupil-shift {
      0%, 100% { transform: translate(-50%,-50%) scaleX(1); }
      30% { transform: translate(-48%,-50%) scaleX(0.85); }
      60% { transform: translate(-52%,-50%) scaleX(1.1); }
      80% { transform: translate(-50%,-48%) scaleX(0.95); }
    }
    @keyframes iris-rotate {
      0% { transform: translate(-50%,-50%) rotate(0deg); }
      100% { transform: translate(-50%,-50%) rotate(360deg); }
    }
    @keyframes text-burn {
      0%, 100% { text-shadow: 0 0 10px rgba(255,80,0,0.5), 0 0 20px rgba(255,40,0,0.3); }
      50% { text-shadow: 0 0 20px rgba(255,100,0,0.8), 0 0 40px rgba(255,60,0,0.5), 0 0 60px rgba(255,40,0,0.3); }
    }
    @keyframes tower-glow {
      0%, 100% { filter: drop-shadow(0 0 8px rgba(255,80,0,0.5)); }
      50% { filter: drop-shadow(0 0 16px rgba(255,120,0,0.8)); }
    }
    @keyframes ember-float-1 {
      0% { transform: translateY(0) translateX(0) scale(1); opacity: 0.8; }
      50% { transform: translateY(-60px) translateX(15px) scale(0.6); opacity: 0.4; }
      100% { transform: translateY(-120px) translateX(-10px) scale(0.2); opacity: 0; }
    }
    @keyframes ember-float-2 {
      0% { transform: translateY(0) translateX(0) scale(1); opacity: 0.6; }
      50% { transform: translateY(-80px) translateX(-20px) scale(0.5); opacity: 0.3; }
      100% { transform: translateY(-140px) translateX(15px) scale(0.1); opacity: 0; }
    }
    @keyframes ember-float-3 {
      0% { transform: translateY(0) translateX(0) scale(1); opacity: 0.7; }
      50% { transform: translateY(-50px) translateX(25px) scale(0.7); opacity: 0.5; }
      100% { transform: translateY(-100px) translateX(-5px) scale(0.3); opacity: 0; }
    }
  `}</style>

{/* Floating embers */}

<div
  style={{
    position: 'absolute',
    top: '45%',
    left: '42%',
    width: '4px',
    height: '4px',
    borderRadius: '50%',
    background: '#ff6600',
    animation: 'ember-float-1 3s ease-out infinite'
  }}
/>
<div
  style={{
    position: 'absolute',
    top: '48%',
    left: '55%',
    width: '3px',
    height: '3px',
    borderRadius: '50%',
    background: '#ff4400',
    animation: 'ember-float-2 4s ease-out infinite 0.5s'
  }}
/>
<div
  style={{
    position: 'absolute',
    top: '44%',
    left: '48%',
    width: '5px',
    height: '5px',
    borderRadius: '50%',
    background: '#ff8800',
    animation: 'ember-float-3 3.5s ease-out infinite 1s'
  }}
/>
<div
  style={{
    position: 'absolute',
    top: '46%',
    left: '52%',
    width: '3px',
    height: '3px',
    borderRadius: '50%',
    background: '#ffaa00',
    animation: 'ember-float-1 4.5s ease-out infinite 1.5s'
  }}
/>
<div
  style={{
    position: 'absolute',
    top: '50%',
    left: '45%',
    width: '4px',
    height: '4px',
    borderRadius: '50%',
    background: '#ff5500',
    animation: 'ember-float-2 3s ease-out infinite 2s'
  }}
/>
<div
  style={{
    position: 'absolute',
    top: '43%',
    left: '58%',
    width: '3px',
    height: '3px',
    borderRadius: '50%',
    background: '#ff7700',
    animation: 'ember-float-3 5s ease-out infinite 0.8s'
  }}
/>

  <div style={{
    position: 'relative',
    width: '100%',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center'
  }}>

    {/* The Eye */}
    <div style={{
      position: 'relative',
      width: '240px',
      height: '130px',
      borderRadius: '50%',
      background: 'radial-gradient(ellipse at center, #ff6a00 0%, #ff4500 30%, #cc2200 60%, #660000 80%, transparent 100%)',
      animation: 'pulse-glow 3s ease-in-out infinite, flicker 2s ease-in-out infinite',
      zIndex: 2
    }}>
      {/* Outer fire ring */}
      <div style={{
        position: 'absolute',
        top: '-8px',
        left: '-8px',
        right: '-8px',
        bottom: '-8px',
        borderRadius: '50%',
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(255,80,0,0.15) 60%, rgba(255,40,0,0.1) 80%, transparent 100%)',
        animation: 'flicker 1.8s ease-in-out infinite 0.3s'
      }} />

      {/* Iris ring */}
      <div style={{
        position: 'absolute',
        top: '50%',
        left: '50%',
        width: '95px',
        height: '95px',
        borderRadius: '50%',
        border: '3px solid rgba(255,200,0,0.6)',
        background: 'radial-gradient(circle, transparent 30%, rgba(255,120,0,0.3) 60%, transparent 100%)',
        animation: 'iris-rotate 8s linear infinite',
        boxShadow: 'inset 0 0 20px rgba(255,150,0,0.5)'
      }} />

      {/* Inner iris detail */}
      <div style={{
        position: 'absolute',
        top: '50%',
        left: '50%',
        width: '60px',
        height: '60px',
        borderRadius: '50%',
        border: '1px solid rgba(255,180,0,0.3)',
        transform: 'translate(-50%,-50%)',
        animation: 'iris-rotate 12s linear infinite reverse'
      }} />

      {/* Pupil slit */}
      <div style={{
        position: 'absolute',
        top: '50%',
        left: '50%',
        width: '8px',
        height: '90px',
        background: 'linear-gradient(to bottom, transparent, #000000 15%, #0a0000 50%, #000000 85%, transparent)',
        borderRadius: '50%',
        animation: 'pupil-shift 4s ease-in-out infinite',
        boxShadow: '0 0 15px 5px rgba(255,60,0,0.8), 0 0 30px 10px rgba(255,40,0,0.4)',
        zIndex: 3
      }} />

      {/* Central glow */}
      <div style={{
        position: 'absolute',
        top: '50%',
        left: '50%',
        transform: 'translate(-50%,-50%)',
        width: '50px',
        height: '50px',
        borderRadius: '50%',
        background: 'radial-gradient(circle, rgba(255,220,0,0.5) 0%, rgba(255,150,0,0.2) 40%, transparent 70%)',
        animation: 'flicker 1.5s ease-in-out infinite'
      }} />
    </div>

    {/* Tower structure */}
    <div style={{
      width: '6px',
      height: '80px',
      background: 'linear-gradient(to bottom, #331100, #1a0800, #0a0400)',
      animation: 'tower-glow 3s ease-in-out infinite',
      zIndex: 1
    }} />
    <div style={{
      width: '30px',
      height: '12px',
      background: 'linear-gradient(to bottom, #221000, #110800)',
      clipPath: 'polygon(30% 0%, 70% 0%, 100% 100%, 0% 100%)',
      animation: 'tower-glow 3s ease-in-out infinite'
    }} />
    <div style={{
      width: '50px',
      height: '15px',
      background: 'linear-gradient(to bottom, #1a0800, #0a0400)',
      clipPath: 'polygon(20% 0%, 80% 0%, 100% 100%, 0% 100%)',
      animation: 'tower-glow 3s ease-in-out infinite'
    }} />

  </div>

{/* Black Speech inscription */}

<p
  style={{
    fontFamily: 'serif',
    fontStyle: 'italic',
    fontSize: '0.9rem',
    color: '#cc8800',
    marginTop: '2.5rem',
    textAlign: 'center',
    animation: 'text-burn 4s ease-in-out infinite',
    letterSpacing: '0.12em',
    maxWidth: '420px',
    lineHeight: '1.8'
  }}
>
  Ash nazg durbatuluk, ash nazg gimbatul,
  <br />
  ash nazg thrakatuluk, agh burzum-ishi krimpatul.
</p>

<p
  style={{
    fontFamily: 'serif',
    fontStyle: 'italic',
    fontSize: '0.8rem',
    color: '#886633',
    marginTop: '0.5rem',
    textAlign: 'center',
    letterSpacing: '0.06em',
    maxWidth: '420px',
    opacity: 0.7,
    lineHeight: '1.7'
  }}
>
  Ein Ring, sie zu knechten, sie alle zu finden,
  <br />
  ins Dunkel zu treiben und ewig zu binden.
</p>

</div>

---

<div style={{ textAlign: 'center', marginTop: '1.5rem', marginBottom: '1rem' }}>

**Built by Claude in under 2 minutes.** Pure CSS. No dependencies. No JavaScript.

_One does not simply mass-produce documentation... unless you have an AI._

</div>

<CardGroup cols={3}>
  <Card title="Geschwindigkeit" icon="bolt">
    Von Prompt zu Production in 120 Sekunden. Kein npm install. Kein Build-Step.
  </Card>
  <Card title="Zero Dependencies" icon="feather">
    Pure CSS Animationen mit Keyframes, Gradients und Box-Shadows.
  </Card>
  <Card title="Mintlify-Native" icon="leaf">
    Direkt in MDX eingebettet. Funktioniert ueberall wo Mintlify laeuft.
  </Card>
</CardGroup>
